<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team4.joopging.mappers.CommuAttachFileMapper">
    <insert id="insert">
        INSERT INTO TBL_COMMUATTACH (UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO)
        VALUES (#{uuid}, #{uploadPath}, #{fileName}, #{image}, #{commuBno})
    </insert>

    <delete id="delete">
        DELETE FROM TBL_COMMUATTACH WHERE UUID = #{uuid}
    </delete>

    <select id="findByBno" resultType="commuAttachFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO
        FROM TBL_COMMUATTACH
        WHERE COMMUBNO = #{commuBno}
    </select>

    <delete id="deleteAll">
        DELETE FROM TBL_COMMUATTACH WHERE COMMUBNO = #{commuBno}
    </delete>

    <select id="getOldFiles" resultType="commuAttachFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO
        FROM TBL_COMMUATTACH
        WHERE UPLOADPATH = TO_CHAR(SYSDATE - 1, 'YYYY/MM/DD')
    </select>
</mapper>