<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team4.joopging.mappers.EventFileMapper">
    <insert id="insert">
        INSERT INTO TBL_EVENTATTACH (UUID, UPLOADPATH, FILENAME, IMAGE, EVENTNUM)
        VALUES (#{uuid}, #{uploadPath}, #{fileName}, #{image}, #{eventNum})
    </insert>

    <delete id="delete">
        DELETE FROM TBL_EVENTATTACH WHERE UUID = #{uuid}
    </delete>

    <select id="findByBno" resultType="eventFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, EVENTNUM
        FROM TBL_EVENTATTACH
        WHERE EVENTNUM = #{eventNum}
    </select>

    <delete id="deleteAll">
        DELETE FROM TBL_EVENTATTACH WHERE EVENTNUM = #{eventNum}
    </delete>

    <select id="getOldFiles" resultType="eventFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, EVENTNUM
        FROM TBL_EVENTATTACH
        WHERE UPLOADPATH = TO_CHAR(SYSDATE - 1, 'YYYY/MM/DD')
    </select>
</mapper>