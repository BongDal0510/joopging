<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team4.joopging.mappers.MypageMapper">

    <!--플로깅 예약 목록-->
<select id="getPloResList" resultType="com.team4.joopging.mypage.vo.PloResVO">

    SELECT ploResNum, memberNum, ploResLocation, ploggingNum, ploResDate,ploResTime,ploResStatus
    FROM(
    <![CDATA[
    SELECT /** INDEX_DESC(TBL_PLO_RES RESERVATION_PK)*/
    ROWNUM R, ploResNum, memberNum, ploResLocation, ploggingNum, ploResDate,ploResTime,ploResStatus
    FROM TBL_PLO_RES WHERE memberNum = #{memberNum}
                ROWNUM <= #{pageNum} * #{amount}
            )
        WHERE R > (#{pageNum} - 1) * #{amount}
                        ]]>
</select>

    <!--내가 예약한 플로깅 내역 전체 개수-->
    <select id="totalPloResCnt" resultType="_int">
        SELECT COUNT(ploResNum) FROM TBL_PLO_RES WHERE memberNum = #{memberNum}
    </select>

<!--찜 목록-->
<select id="getGoodsLikeList" resultType="com.team4.joopging.mypage.vo.GoodsLikeListVO">
SELECT goodsLikeListNum, memberNum, goodsNum
FROM(
    <![CDATA[
SELECT /** INDEX_DESC(TBL_GOODS_LIKE_LIST GOODS_LIKE_LIST_PK)*/
ROWNUM R,goodsLikeListNum, memberNum, goodsNum
FROM TBL_GOODS_LIKE_LIST
WHERE memberNum = #{memberNum}
                ROWNUM <= #{pageNum} * #{amount}
            )
        WHERE R > (#{pageNum} - 1) * #{amount}
                        ]]>
</select>

    <!--내가 찜한 목록 전체 개수-->
    <select id="totalGoodsLikeCnt" resultType="_int">
        SELECT COUNT(goodsLikeListNum) FROM TBL_GOODS_LIKE_LIST WHERE memberNum = #{memberNum}
    </select>

<!--플로깅 예약 취소-->
<update id="deleteReservation">
UPDATE TBL_RESERVATION
SET reservationStatus = 1
WHERE ploResNum = #{ploResNum}
</update>

<!--아이디로 멤버 번호 찾기-->
<select id="seletMemberNum" resultType="_int">
    SELECT memberNum FROM TBL_MEMBER WHERE memberId = #{memberId}
</select>

<!--멤버 정보 수정-->
<update id="updateMember">
    UPDATE TBL_MEMBER
    SET memberPw = #{memberPw}, memberEmail = #{memberEmail}, memberZipcode = #{memberZipcode}, memberAddress = #{memberAddress}
    WHERE memberNum = #{memberNum}
</update>

<!--멤버 삭제-->
<update id="deleteMember">
    UPDATE TBL_MEMBER
    SET status = 1
    WHERE memberId = #{memberId} AND memberPw = #{memberPw}
</update>


</mapper>