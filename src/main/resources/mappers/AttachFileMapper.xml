<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team4.joopging.mappers.AttachFileMapper">
    <insert id="insert">
        INSERT INTO TBL_ATTACH (UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO)
        VALUES (#{uuid}, #{uploadPath}, #{fileName}, #{image}, #{commuBno})
    </insert>

    <delete id="delete">
        DELETE FROM TBL_ATTACH WHERE UUID = #{uuid}
    </delete>

    <select id="findByBno" resultType="attachFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO
        FROM TBL_ATTACH
        WHERE COMMUBNO = #{commuBno}
    </select>

    <delete id="deleteAll">
        DELETE FROM TBL_ATTACH WHERE COMMUBNO = #{commuBno}
    </delete>

    <select id="getOldFiles" resultType="attachFileVO">
        SELECT UUID, UPLOADPATH, FILENAME, IMAGE, COMMUBNO
        FROM TBL_ATTACH
        WHERE UPLOADPATH = TO_CHAR(SYSDATE - 1, 'YYYY/MM/DD')
    </select>
</mapper>